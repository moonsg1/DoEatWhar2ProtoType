<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 앱 전체 테마 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/eatingActionBar</item>
        <item name="android:actionBarItemBackground">@color/actionBarItemBg</item>
    </style>

    <!-- 액션바 스타일 -->
    <style name="eatingActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/topBar_bg</item>
    </style>

</resources>